---
phase: 06-content-and-polish
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/App.tsx
  - src/components/XtermTerminal.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Secondary pages load lazily via React.lazy()"
    - "Dashboard loads eagerly (primary route)"
    - "xterm.js WebGL renderer verified active"
    - "xterm.js fit addon handles container resize"
    - "No micro-optimization performed (per D-15)"
  artifacts:
    - path: "src/App.tsx"
      provides: "React.lazy wrappers for non-critical routes + Suspense"
      contains: "React.lazy(() => import"
    - path: "src/components/XtermTerminal.tsx"
      provides: "Verified WebGL + fit addon integration"
      contains: "WebglAddon"
  key_links:
    - from: "App.tsx React.lazy imports"
      to: "Pages (Leaderboards, Settings, etc.)"
      via: "lazy(() => import('./pages/...'))"
      pattern: "React.lazy"
---

<objective>
Optimize bundle size and verify terminal rendering performance.

Purpose: Reduce initial load time by lazy-loading non-critical pages. Verify that xterm.js WebGL renderer is actively used and fit addon properly handles terminal resize. No micro-optimization per D-15.

Output: Modified App.tsx with React.lazy for secondary pages. Verified XtermTerminal.tsx WebGL + fit addon integration.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-content-and-polish/06-CONTEXT.md (D-13 to D-15)
@src/App.tsx (lines 1-50 -- current routes and imports)
@src/components/XtermTerminal.tsx (lines 1-167 -- current xterm.js setup)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lazy-load non-critical routes via React.lazy() with Suspense</name>
  <files>src/App.tsx</files>

  <read_first>
  Read src/App.tsx lines 1-50 (entire file). Note current pattern: all pages are statically imported. Dashboard (route "/") should remain eager-loaded.
  </read_first>

  <action>
  Edit src/App.tsx:

  KEEP this static import:
  ```
  import { Dashboard } from './pages/Dashboard';
  ```

  Replace all other static page imports (lines 5-16) with React.lazy:
  ```
  const Operations = React.lazy(() => import('./pages/Operations'));
  const Character = React.lazy(() => import('./pages/Character'));
  const Equipment = React.lazy(() => import('./pages/Equipment'));
  const Marketplace = React.lazy(() => import('./pages/Marketplace'));
  const Leaderboards = React.lazy(() => import('./pages/Leaderboards'));
  const Settings = React.lazy(() => import('./pages/Settings'));
  const AIAutoplay = React.lazy(() => import('./pages/AIAutoplay'));
  const Quests = React.lazy(() => import('./pages/Quests'));
  const GuildManagement = React.lazy(() => import('./pages/GuildManagement'));
  const AICompanionHub = React.lazy(() => import('./pages/AICompanionHub'));
  const SocialDashboard = React.lazy(() => import('./pages/SocialDashboard'));
  const CrossPlatformSync = React.lazy(() => import('./pages/CrossPlatformSync'));
  ```

  Add Suspense wrapper around Routes. Structure should be:
  ```
  <Layout>
    <React.Suspense fallback={<div className="flex items-center justify-center h-full"><div className="text-cyber-primary font-mono animate-pulse">Loading...</div></div>}>
      <Routes>
        <Route path="/" element={<Dashboard />} />
        ...
      </Routes>
    </React.Suspense>
  </Layout>
  ```

  IMPORTANT: Do NOT change route paths, element names, AchievementPopup, or createAchievementChecker.
  </action>

  <verify>
  <automated>node -e "const fs=require('fs');const a=fs.readFileSync('src/App.tsx','utf8');console.log('lazy:',a.includes('React.lazy'),'suspense:',a.includes('React.Suspense'),'dashboard eager:',a.includes('import { Dashboard }'),'old removed:',!a.includes('import { Leaderboards }'));process.exit(a.includes('React.lazy')&&a.includes('React.Suspense')?0:1)</automated>
  </verify>

  <acceptance_criteria>
  1. Dashboard remains static import
  2. All other pages use React.lazy(() => import('./pages/...'))
  3. Routes wrapped in React.Suspense with loading fallback
  4. npx tsc --noEmit src/App.tsx passes
  </acceptance_criteria>

  <done>
  App.tsx uses React.lazy for non-Dashboard pages with Suspense.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify xterm.js WebGL renderer + fit addon</name>
  <files>src/components/XtermTerminal.tsx</files>

  <read_first>
  Read src/components/XtermTerminal.tsx lines 1-167. The existing code already loads WebGLAddon with Canvas fallback and FitAddon. Confirm these are present.
  </read_first>

  <action>
  Edit src/components/XtermTerminal.tsx:

  CONFIRM existing code has (do NOT change):
  - WebglAddon loaded at ~line 71
  - CanvasAddon fallback on context loss (~lines 72-78)
  - FitAddon loaded at ~line 66
  - fitAddon.fit() called at ~line 82
  - ResizeObserver with requestAnimationFrame at ~lines 139-146

  ADD after the ResizeObserver block (after line 146):
  ```
  // Re-fit when custom font loads
  document.fonts.ready.then(() => {
    requestAnimationFrame(() => {
      fitAddonRef.current?.fit();
    });
  });

  // Log renderer type for debugging
  console.log('[XtermTerminal] WebGL renderer active (Canvas fallback available)');
  ```

  IMPORTANT: Only ADD lines. Do not change existing code.
  </action>

  <verify>
  <automated>node -e "const fs=require('fs');const x=fs.readFileSync('src/components/XtermTerminal.tsx','utf8');const c=[x.includes('WebglAddon'),x.includes('FitAddon'),x.includes('fitAddon.fit()'),x.includes('document.fonts.ready'),x.includes('WebGL renderer')];console.log('OK:',c.every(Boolean));process.exit(c.every(Boolean)?0:1)</automated>
  </verify>

  <acceptance_criteria>
  1. Existing WebGL + FitAddon code preserved unchanged
  2. NEW: document.fonts.ready re-fit added
  3. NEW: console.log confirms WebGL active
  4. npx tsc --noEmit passes
  </acceptance_criteria>

  <done>
  XtermTerminal.tsx verified. WebGL active, fit handles resize, font re-fit added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

React.lazy is standard React code-splitting. xterm.js addons run within existing sandbox. No new boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------+----------+-----------+-------------+-----------------|
| T-06-05-01 | DoS | React.lazy | accept | Standard API; chunks on demand |
| T-06-05-02 | EoP | xterm.js | accept | Existing dep; no new addons |
</threat_model>

<verification>
1. npm run build succeeds
2. npx tsc --noEmit passes on both files
</verification>

<success_criteria>
- Dashboard loads eagerly (initial bundle)
- Secondary pages load as separate chunks
- xterm.js WebGL active (console log)
- Terminal re-fits after font load
- No performance regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-and-polish/06-05-SUMMARY.md`
</output>
