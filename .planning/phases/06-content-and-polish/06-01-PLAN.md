---
phase: 06-content-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/gameStore.ts
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Player sees 20 target systems instead of 5"
    - "Player can purchase 20 upgrades instead of 11"
    - "New targets organized across 4 difficulty tiers (Entry/Mid/High/Elite)"
    - "New upgrades include 4 hardware and 5 software items"
    - "All new content uses existing Target and Equipment interfaces — no new types"
  artifacts:
    - path: "src/store/gameStore.ts"
      provides: "Expanded initialTargets[] to 20 entries"
      contains: "id: 'target-20'"
    - path: "src/store/gameStore.ts"
      provides: "Expanded initialEquipment[] to 20 entries"
      contains: "id: 'eq-20'"
  key_links:
    - from: "gameStore.ts initialTargets[]"
      to: "hack command in hacking.ts"
      via: "store.targets selector"
      pattern: "targets.find"
    - from: "gameStore.ts initialEquipment[]"
      to: "UpgradePanel, equipment command"
      via: "store.equipment selector"
      pattern: "equipment.find"
---

<objective>
Expand game content — 15 new target systems and 9 new upgrades.

Purpose: Deepen the mid-game and end-game experience. The current 5 targets and 11 upgrades exhaust too quickly. The new content fills progression gaps and gives players meaningful choices through the full play session.

Output: Modified `initialTargets[]` (20 entries, 4 tiers) and `initialEquipment[]` (20 entries, 4 hardware + 5 software added) in `src/store/gameStore.ts`. No new types, no new components — pure data extension per D-12.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-content-and-polish/06-CONTEXT.md
@src/store/gameStore.ts (lines 606-668)

<interfaces>
From src/store/gameStore.ts:
```typescript
export interface Target {
  id: string;
  name: string;
  type: 'corporation' | 'government' | 'individual' | 'criminal';
  difficulty: number;
  securityLevel: number;
  rewards: {
    credits: number;
    experience: number;
    reputation: number;
  };
  unlocked: boolean;
}

export interface Equipment {
  id: string;
  name: string;
  type: 'hardware' | 'software';
  level: number;
  bonus: number;
  equipped: boolean;
  upgradeCost: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 15 new target systems across 4 difficulty tiers</name>
  <files>src/store/gameStore.ts</files>

  <read_first>
  Read src/store/gameStore.ts lines 606-652 (existing initialTargets[] array). Read lines 64-76 (Target interface). The existing 5 targets end with target-5 (Government Data Vault, diff 5). New targets use sequential IDs target-6 through target-20.
  </read_first>

  <action>
  **File:** src/store/gameStore.ts
  **Location:** Insert after the existing target-5 entry (ending at line 651 `},`), before the closing `];` on line 652.
  **Format:** Same 2-space indent, trailing commas as existing entries.

  Add these 15 entries in this order:

  Entry Tier (4 targets, difficulties 1-2):
  1. `{ id: 'target-6', name: 'IoT Botnet Controller', type: 'criminal', difficulty: 1, securityLevel: 1, rewards: { credits: 60, experience: 30, reputation: 2 }, unlocked: false }`
  2. `{ id: 'target-7', name: 'Smart City Traffic Hub', type: 'government', difficulty: 1, securityLevel: 2, rewards: { credits: 80, experience: 40, reputation: 2 }, unlocked: false }`
  3. `{ id: 'target-8', name: 'Crypto Mining Rig Cluster', type: 'individual', difficulty: 2, securityLevel: 2, rewards: { credits: 200, experience: 100, reputation: 5 }, unlocked: false }`
  4. `{ id: 'target-9', name: 'Suburban Mesh Network', type: 'individual', difficulty: 2, securityLevel: 3, rewards: { credits: 250, experience: 120, reputation: 5 }, unlocked: false }`

  Mid Tier (4 targets, difficulties 3-4):
  5. `{ id: 'target-10', name: 'Dark Net Marketplace', type: 'criminal', difficulty: 3, securityLevel: 4, rewards: { credits: 400, experience: 200, reputation: 10 }, unlocked: false }`
  6. `{ id: 'target-11', name: 'Corporate VPN Gateway', type: 'corporation', difficulty: 3, securityLevel: 3, rewards: { credits: 350, experience: 180, reputation: 10 }, unlocked: false }`
  7. `{ id: 'target-12', name: 'Hospital Patient Records', type: 'corporation', difficulty: 4, securityLevel: 5, rewards: { credits: 600, experience: 350, reputation: 15 }, unlocked: false }`
  8. `{ id: 'target-13', name: 'Satellite ISP Backbone', type: 'corporation', difficulty: 4, securityLevel: 6, rewards: { credits: 1000, experience: 500, reputation: 25 }, unlocked: false }`

  High Tier (4 targets, difficulties 5-6):
  9. `{ id: 'target-14', name: 'Military Drone Uplink', type: 'government', difficulty: 5, securityLevel: 7, rewards: { credits: 2500, experience: 1200, reputation: 60 }, unlocked: false }`
  10. `{ id: 'target-15', name: 'Central Bank SWIFT Terminal', type: 'corporation', difficulty: 5, securityLevel: 8, rewards: { credits: 3000, experience: 1500, reputation: 70 }, unlocked: false }`
  11. `{ id: 'target-16', name: 'AI Research Lab Core', type: 'corporation', difficulty: 6, securityLevel: 8, rewards: { credits: 5000, experience: 2500, reputation: 100 }, unlocked: false }`
  12. `{ id: 'target-17', name: 'Defense Contractor Mainframe', type: 'corporation', difficulty: 6, securityLevel: 9, rewards: { credits: 6000, experience: 3000, reputation: 120 }, unlocked: false }`

  Elite Tier (3 targets, difficulties 7-8):
  13. `{ id: 'target-18', name: 'Extraterrestrial Signal Array', type: 'government', difficulty: 7, securityLevel: 9, rewards: { credits: 10000, experience: 5000, reputation: 200 }, unlocked: false }`
  14. `{ id: 'target-19', name: 'NSA Black Site Server', type: 'government', difficulty: 7, securityLevel: 10, rewards: { credits: 15000, experience: 7500, reputation: 300 }, unlocked: false }`
  15. `{ id: 'target-20', name: 'Quantum Encryption Node', type: 'government', difficulty: 8, securityLevel: 10, rewards: { credits: 25000, experience: 10000, reputation: 500 }, unlocked: false }`

  CRITICAL: Do NOT modify any existing target. Do NOT change anything else in the file. All new targets have `unlocked: false` — they get unlocked progressively by the hack chain in hacking.ts (target-1 unlocks target-2, etc.).
  </action>

  <verify>
  <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/store/gameStore.ts','utf8');const ids=[...s.matchAll(/id:\s*'target-(\d+)'/g)].map(m=>parseInt(m[1]));console.log('Target count:',ids.length);if(ids.length!==20||Math.max(...ids)!==20){process.exit(1)}"</automated>
  </verify>

  <acceptance_criteria>
  1. `node -e "const fs=require('fs');const s=fs.readFileSync('src/store/gameStore.ts','utf8');const ids=[...s.matchAll(/id:\s*'target-(\d+)'/g)].map(m=>parseInt(m[1]));console.log(ids.length,Math.max(...ids));process.exit(ids.length===20&&Math.max(...ids)===20?0:1)"` prints "20 20" and exits 0
  2. File compiles: TypeScript check passes on gameStore.ts (no new types introduced)
  3. New targets span difficulties 1-8 (verify at least one target-* with difficulty 8 exists)
  4. Every new target has unlocked: false (not accidentally unlocked)
  </acceptance_criteria>

  <done>
  initialTargets[] has exactly 20 entries. IDs sequential target-1 through target-20. All 4 tiers present. No existing targets modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 9 new equipment upgrades (4 hardware, 5 software)</name>
  <files>src/store/gameStore.ts</files>

  <read_first>
  Read src/store/gameStore.ts lines 654-668 (existing initialEquipment[] array). Read lines 35-43 (Equipment interface). Existing equipment ends with eq-11 (Zero-Day Suite). New items use IDs eq-12 through eq-20.
  </read_first>

  <action>
  **File:** src/store/gameStore.ts
  **Location:** Insert after the existing eq-11 entry (line 667), before the closing `];` on line 668.
  **Format:** Same as existing entries (2-space indent, line breaks same style).

  Insert these 9 entries in the order listed:

  **New Hardware** (4 items — slot between existing entries by cost):
  1. `{ id: 'eq-12', name: 'Cooling System', type: 'hardware', level: 1, bonus: 8, equipped: false, upgradeCost: 300 }`
  2. `{ id: 'eq-13', name: 'Network Switch', type: 'hardware', level: 1, bonus: 20, equipped: false, upgradeCost: 4000 }`
  3. `{ id: 'eq-14', name: 'FPGA Array', type: 'hardware', level: 1, bonus: 50, equipped: false, upgradeCost: 20000 }`
  4. `{ id: 'eq-15', name: 'Neural Interface', type: 'hardware', level: 1, bonus: 120, equipped: false, upgradeCost: 100000 }`

  **New Software** (5 items — slot between existing entries by cost):
  5. `{ id: 'eq-16', name: 'Packet Injector', type: 'software', level: 1, bonus: 5, equipped: false, upgradeCost: 600 }`
  6. `{ id: 'eq-17', name: 'Log Cleaner', type: 'software', level: 1, bonus: 14, equipped: false, upgradeCost: 3000 }`
  7. `{ id: 'eq-18', name: 'Port Scanner', type: 'software', level: 1, bonus: 30, equipped: false, upgradeCost: 12000 }`
  8. `{ id: 'eq-19', name: 'AI Training Model', type: 'software', level: 1, bonus: 70, equipped: false, upgradeCost: 50000 }`
  9. `{ id: 'eq-20', name: 'Backdoor Installer', type: 'software', level: 1, bonus: 180, equipped: false, upgradeCost: 200000 }`

  **Progression verification — existing bonus sequence:**
  Hardware: Laptop(2)→Cooling(8)→Rack(5)→Switch(20)→GPU(12)→... No — the items are independent purchases, not a sorted chain. But insert at end after Satellite Uplink is correct since these are new unlocks that should be purchased later in progression.

  CRITICAL: Do NOT modify any existing equipment. Do NOT change anything else in the file.
  </action>

  <verify>
  <automated>node -e "const fs=require('fs');const s=fs.readFileSync('src/store/gameStore.ts','utf8');const ids=[...s.matchAll(/id:\s*'eq-(\d+)'/g)].map(m=>parseInt(m[1]));console.log('Equipment count:',ids.length, 'Max ID:',Math.max(...ids));if(ids.length!==20||Math.max(...ids)!==20){process.exit(1)}"</automated>
  </verify>

  <acceptance_criteria>
  1. `node -e "const fs=require('fs');const s=fs.readFileSync('src/store/gameStore.ts','utf8');const hwIds=[...s.matchAll(/id:\s*'eq-(\d+)'[^}]*type:\s*'hardware'/g)];const swIds=[...s.matchAll(/id:\s*'eq-(\d+)'[^}]*type:\s*'software'/g)];console.log('Hardware:',hwIds.length,'Software:',swIds.length);if(hwIds.length!==10||swIds.length!==10){process.exit(1)}"` — must print "Hardware: 10 Software: 10"
  2. File compiles with no type errors
  3. New equipment IDs are sequential eq-12 through eq-20
  4. All new items have equipped: false
  </acceptance_criteria>

  <done>
  initialEquipment[] has exactly 20 entries. IDs sequential eq-1 through eq-20. 10 hardware, 10 software. No existing items modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. All data is static arrays in client-side TypeScript. Existing Target and Equipment interfaces unchanged. No new user input, API calls, or state flows.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------+----------+-----------+-------------+-----------------|
| T-06-01-01 | Spoofing | gameStore.ts | accept | Static data arrays — no trust boundary crossed |
| T-06-01-02 | Tampering | gameStore.ts | accept | Static data only; client-side Zustand store |
</threat_model>

<verification>
1. `node -e "const fs=require('fs');const s=fs.readFileSync('src/store/gameStore.ts','utf8');..."` confirms 20 targets and 20 equipment entries
2. `npx tsc --noEmit src/store/gameStore.ts` passes with no errors (or `npm run build` succeeds)
</verification>

<success_criteria>
- initialTargets[] has 20 entries spanning difficulties 1-8
- initialEquipment[] has 20 entries (10 hardware, 10 software)
- Build succeeds with no type errors
- Hacking chain mechanic correctly unlocks targets sequentially (hacking.ts line 93-103 uses findIndex + 1, works automatically with larger array)
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-and-polish/06-01-SUMMARY.md`
</output>
